<div class="mb-6">
  <div class="flex flex-row justify-between">
    <div class="flex gap-2 flex-wrap">
      <% current_status = params[:status].presence %>

      <%= button_tag 'Todos',
          class: link_classes.call(current_status.blank?),
          data: {
            action: "click->candidates#filterByStatus",
            status: "",
            candidates_target: "filterButton"
          }
        %>

      <% Candidate::Status.to_a.each do |status| %>
          <%= button_tag status.first,
            type: 'button',
            class: link_classes.call(current_status == status.last),
            data: {
              action: "click->candidates#filterByStatus",
              status: status.last,
              candidates_target: "filterButton"
            }
          %>
        <% end %>
    </div>

    <%= render 'form_search' %>
  </div>
</div>
