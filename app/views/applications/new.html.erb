<main class="w-full mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
    <div class="rounded-xl border border-slate-200 bg-white p-6 mb-8">
    <h1 class="text-2xl font-bold text-slate-900 mb-6"><%= t('applications.new.role_name', role: @recruitment_role.role.name) %></h1>
    <span>
      <%= simple_format(@recruitment_role.role.description, class: "mt-2 text-slate-900 break-words") %>
    </span>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-6 mb-8">
    <%= render 'form' %>
  </div>
</main>
